#class_name Summon
#extends Node2D
#
#@export var stats: SummonResource
#@export var summoned_item_scene:PackedScene
#
## Called when the node enters the scene tree for the first time.
#func _ready():
	#var new_item = summon_scene.instantiate()
	#new_item.stats = stats
	#item_holder.add_child(new_item)
	#var summon_texture = SummonTexture.new()
	#summon_texture.stats = stats
	#add_child(summon_texture)
	#summon_texture.position = -0.5*stats.image.get_size()

